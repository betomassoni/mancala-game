version: "2.1"

services:
#  api:
#    image: robertomassoni/mancala-game
#    build:
#      context: .
#    restart: on-failure
#    ports:
#      - "8081:8080"
#    networks:
#      - web

  redis:
    image: redis:latest
    restart: on-failure
    ports:
      - "6379:6379"
    networks:
      - web

networks:
  web:
    driver: bridge
